package WrapperClasses;

public class info {
    /*
        Primitive data type ---> (int ,char , byte , short)fast, raw data
        Wrapper classes --> Integer, Character , Byte , Double -> it "Wraps" the
        raw data into object so it can be treated like other object

        Why do we need wrapper classes:
        Primitive data types are faster , then why we need it:
        1. Collection only store objects
        2. Null values
        3. Utility method
        4. Multithreading

        Mapping : every primitive corresponding Wrapper class
        int --> Integer
        short --> Short
        boolean --> Boolean
        char --> Char

        Note : java is object-oriented , needed object from primitive --> wrapper classes

     */
}
